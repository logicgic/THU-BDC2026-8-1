# AGENTS.md instructions
- 本项目是大数据挑战赛的比赛代码，从官方提供的示例项目来进行。

- 回答问题尽可能简洁，不要太多语气词。

## 比赛建模红线
- 涉及量化建模、回测、验证集划分或特征工程时，优先检查未来函数、数据泄漏、幸存者偏差和评测过拟合。
- 日期 `t` 的特征只能使用在决策时点已经公开且可获得的数据。禁止使用未来价格、未来收益、负向 `shift` 生成的特征、未来复权因子、事后修订数据或测试期统计量。
- 标签只用于监督信号，不得进入特征、筛选、缺失值填充、标准化、特征选择或样本权重的拟合过程。
- 所有数据驱动的预处理器（标准化、归一化、填充、编码、PCA、特征选择等）只能在训练集上 `fit`，验证集和测试集只能 `transform`。
- 横截面特征只能使用同一决策日当时可见的股票数据；滚动统计必须按股票、按日期升序计算，并检查窗口边界。

## 时间切分与回测
- 禁止随机划分时间序列。采用按日期切分、滚动验证或扩展窗口验证，测试集始终晚于训练集。
- 当前标签使用未来第1日至第5日开盘收益；训练集末端与验证集目标区间之间至少隔离完整标签跨度，避免跨边界标签泄漏。验证集可携带历史特征上下文，但上下文行不得作为验证目标重复计分。
- 股票池应使用对应历史时点已知的沪深300成分股。若数据只包含当前成分股，必须明确记录幸存者偏差，不得声称是无偏历史回测。
- 明确并保持预测时点、成交价格、持有期和收益标签口径一致；不得用当日收盘后才知道的数据模拟当日开盘成交。
- 回测应考虑停牌、涨跌停、缺失交易日、退市、复权口径和不可成交股票；禁止用向后填充引入未来值。
- 除比赛分数外，至少关注跨时间段稳定性、换手率、最大回撤和极端行情表现，避免只优化单一验证区间。

## 防止评测过拟合
- 不得根据公开榜单反复调参并将榜单当验证集；保留未参与调参的本地最终验证期，并记录每次实验的假设、配置和结果。
- 与基线比较时必须使用相同数据范围、切分、股票池、交易口径和随机种子；优先验证多个时间窗口，避免偶然高分。
- 禁止读取、推断或硬编码隐藏测试标签、测试期答案或特定日期股票；禁止修改官方评分脚本来制造高分。

## 数据与代码可靠性
- 读取股票代码时按字符串处理并保留前导零；日期统一解析、排序、去重，并断言每个“股票代码 + 日期”唯一。
- 训练前检查列类型、缺失率、无穷值、异常价格、重复行和时间范围；任何丢弃、填充或截断策略须在训练与推理中保持一致。
- 训练和推理必须复用相同的特征列、顺序、窗口长度、股票ID映射和预处理器；加载模型时校验配置与权重兼容。
- 固定并记录 Python、NumPy、PyTorch 和数据加载随机种子；保存配置、依赖版本、模型、预处理器、代码版本和指标，保证结果可复现。
- 大规模特征与模型改动先用小样本完成冒烟测试，关注 CPU、GPU、内存、磁盘和运行时限制，避免全量任务 OOM 或超时。
- 不提交密钥、账号、私有数据或无关的大模型/日志产物；外部数据必须符合比赛规则并记录来源、时间范围和处理方式。

## 提交前检查
- 不破坏官方 `train.sh`、`test.sh`、Docker 构建和离线运行入口；不得假设评测环境可联网或拥有本机绝对路径。
- `output/result.csv` 必须包含 `stock_id,weight`，股票代码合法且不重复，最多5只，权重为有限数且总和在 `[0, 1]`；当前方案输出5只时应校验权重和接近1。
- 提交前在干净环境执行训练/推理冒烟测试和官方测试脚本，确认产物路径、编码、列名、模型文件、依赖和容器资源均符合要求。
- 修改核心数据、特征、切分、标签或评分逻辑时，必须增加相应断言或测试，并说明是否影响历史实验的可比性。


## git规范
如果要写git commit的内容，使用约定式提交，内容使用中文。